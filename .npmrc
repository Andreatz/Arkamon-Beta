# Impedisci a npm install di modificare automaticamente le versioni esatte.
# Le versioni in package.json sono state scelte con attenzione per
# garantire compatibilità tra Vite, plugin-react e Tailwind.
save-exact=true

# Riduci i warning di funding e audit (non sopprime i veri errori)
fund=false
audit-level=high
